import { TipoMedidaAreaPipe } from './tipo-medida-area.pipe';

describe('TipoMedidaAreaPipe', () => {
  it('create an instance', () => {
    const pipe = new TipoMedidaAreaPipe();
    expect(pipe).toBeTruthy();
  });
});
